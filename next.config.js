/** @type {import('next').NextConfig} */
const nextConfig = {
  // Images configuration can be extended to allow remote domains if needed.
  images: {
    domains: [],
  },
};

module.exports = nextConfig;
